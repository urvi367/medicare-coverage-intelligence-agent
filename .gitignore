.env
__pycache__/
*.pyc
*.pyo
.chroma/
logs/
.venv/
venv/
agent/
*.docx

# data — commit index + raw CMS data, ignore eval/pubmed artifacts
data/pubmed_raw.json
data/golden_dataset.json
data/golden_gap.json
data/gap_questions.json
data/_backup_preingest/
data/_*.log

# scripts — keep reusable tooling (*.py), ignore large per-run review dumps
scripts/_*.txt

# Vector index — regenerable build artifact, too large for git (rebuild via
# src.rag.indexer + src.rag.pubmed_indexer)
data/chroma/
data/golden_lcd.json
